# 薪资对比分析设计方案

## 1. 目标

围绕当前项目，新增 3 个直接可落地的能力：

1. 城市薪资对比页
2. 行业薪资对比页
3. 一个统一的对比分析服务层

设计原则：

- 优先复用现有 `analysis_service.py`、`analysis_controller.py`、`frontend/src/api/analysis.js`
- 优先使用 Elasticsearch 聚合，不走 PostgreSQL 实时统计
- 页面目标不是“堆图表”，而是“给求职决策提供明确结论”
- 几天内可上线，适合面试演示

---

## 2. 当前项目可复用基础

### 2.1 后端现状

已有数据字段足以支撑对比分析：

- `salary_min`
- `salary_max`
- `city_code`
- `industry_code`
- `experience`
- `education`
- `created_at`
- `skills`
- `ai_skills`

现有文件：

- `jobCollectionWebApi/services/analysis_service.py`
- `jobCollectionWebApi/api/v1/endpoints/analysis_controller.py`
- `jobCollectionWebApi/schemas/analysis_schema.py`

### 2.2 前端现状

已有分析 API 和图表页面能力：

- `frontend/src/api/analysis.js`
- `frontend/src/views/JobAnalysis.vue`
- `frontend/src/views/MajorAnalysis.vue`

当前前端已经具备：

- 筛选区
- 图表渲染
- ECharts 按需引入
- 独立路由页承载能力

---

## 3. 产品范围

## 3.1 城市薪资对比页

用途：

- 对比同一岗位关键词在两个城市的薪资与招聘结构差异
- 面向“去哪座城市找工作更合适”的决策场景

建议入口：

- 路由：`/compare/cities`

建议筛选项：

- 岗位关键词 `keyword`
- 左侧城市 `left_city_code`
- 右侧城市 `right_city_code`
- 经验 `experience`
- 学历 `education`
- 一级行业 `industry`
- 二级行业 `industry_2`
- 时间范围 `days`，默认 30 天

页面模块：

1. 顶部筛选区
2. 左右城市概览卡
3. 薪资核心指标对比
4. 薪资分布对比图
5. 最近 30 天岗位量趋势图
6. 学历/经验结构对比
7. 热门技能对比
8. AI 总结卡

---

## 3.2 行业薪资对比页

用途：

- 对比同一岗位关键词在两个行业中的薪资与门槛差异
- 面向“去哪个行业更值”的决策场景

建议入口：

- 路由：`/compare/industries`

建议筛选项：

- 岗位关键词 `keyword`
- 左侧行业 `left_industry_code`
- 右侧行业 `right_industry_code`
- 城市 `city_code`
- 经验 `experience`
- 学历 `education`
- 时间范围 `days`

页面模块：

1. 顶部筛选区
2. 左右行业概览卡
3. 薪资核心指标对比
4. 薪资分布对比图
5. 岗位数量趋势图
6. 学历/经验结构对比
7. 热门技能对比
8. AI 总结卡

注意：

- 行业页建议第一期只支持一级行业
- 样本量不足的行业不要展示详细结论

---

## 4. 核心指标设计

不建议只看平均薪资。对外主打以下指标：

1. `sample_size`
2. `salary_avg`
3. `salary_median`
4. `salary_p25`
5. `salary_p75`
6. `high_salary_ratio`
7. `job_count_30d`

说明：

- `salary_avg` 用于直观展示
- `salary_median` 作为主指标，抗极端值更强
- `salary_p25/p75` 展示薪资区间离散程度
- `high_salary_ratio` 可定义为 `salary_min >= 25000` 的岗位占比
- `sample_size` 用于结果可信度说明

第一期建议阈值：

- `sample_size < 50`：仅展示基础数据，不生成 AI 结论
- `sample_size < 20`：页面提示“样本不足”

---

## 5. 统一对比分析服务层设计

## 5.1 设计目标

城市页和行业页本质上只是“对比维度不同”。不应该写两套统计逻辑。

建议新增统一服务层：

- 文件：`jobCollectionWebApi/services/comparison_analysis_service.py`

或保守一点，先直接合并到现有：

- `jobCollectionWebApi/services/analysis_service.py`

从工程可维护性看，建议独立一个服务文件，避免继续把 `analysis_service.py` 堆得更重。

---

## 5.2 服务层职责

统一服务层负责：

1. 组装公共过滤条件
2. 组装左/右两侧对比对象过滤条件
3. 生成 ES 聚合 DSL
4. 解析 ES 聚合结果
5. 输出统一响应结构
6. 做缓存
7. 对样本量不足场景做兜底

---

## 5.3 核心方法建议

```python
class ComparisonAnalysisService:
    async def compare_cities(...)
    async def compare_industries(...)
    async def compare_dimension(...)
    async def _build_common_filters(...)
    async def _build_subject_filter(...)
    async def _build_compare_query(...)
    async def _parse_compare_response(...)
```

建议真正复用的核心入口是：

```python
compare_dimension(
    dimension: str,
    left_value: int,
    right_value: int,
    keyword: str | None = None,
    city_code: int | None = None,
    industry_code: int | None = None,
    industry_2_code: int | None = None,
    experience: str | None = None,
    education: str | None = None,
    days: int = 30,
)
```

其中：

- `dimension=city`
- `dimension=industry`

---

## 5.4 公共过滤条件

公共过滤条件适用于左右两侧：

- `keyword`
- `experience`
- `education`
- `days`
- 城市页中的行业过滤
- 行业页中的城市过滤
- `is_active = true` 如果 ES 中有该字段可同步使用

建议规则：

- `keyword` 用 `multi_match`
- `experience` / `education` 用 `term` 或 `prefix`
- 时间范围用 `created_at >= now - days`

---

## 5.5 ES 查询结构

建议使用单次查询完成左右对比，避免两次 ES round-trip。

推荐 DSL 结构：

```json
{
  "size": 0,
  "track_total_hits": true,
  "query": {
    "bool": {
      "filter": [
        { "range": { "created_at": { "gte": "now-30d/d" } } }
      ],
      "must": [
        {
          "multi_match": {
            "query": "java 后端",
            "fields": ["title^2", "description", "major_name"]
          }
        }
      ]
    }
  },
  "aggs": {
    "left": {
      "filter": { "term": { "city_code": 101020100 } },
      "aggs": {}
    },
    "right": {
      "filter": { "term": { "city_code": 101280100 } },
      "aggs": {}
    }
  }
}
```

左右聚合子项建议一致：

1. `salary_stats`
2. `salary_percentiles`
3. `salary_ranges`
4. `trend_daily`
5. `education_dist`
6. `experience_dist`
7. `top_skills`
8. `top_ai_skills`

---

## 5.6 聚合建议

### 5.6.1 薪资统计

用 `salary_min` 做第一期统计即可，和你当前项目一致。

```json
"salary_stats": {
  "stats": { "field": "salary_min" }
}
```

### 5.6.2 薪资分位数

```json
"salary_percentiles": {
  "percentiles": {
    "field": "salary_min",
    "percents": [25, 50, 75]
  }
}
```

### 5.6.3 薪资分布

沿用现有薪资桶，便于和当前首页保持一致：

- 10k 以下
- 10k-15k
- 15k-25k
- 25k-35k
- 35k 以上

### 5.6.4 趋势

```json
"trend_daily": {
  "date_histogram": {
    "field": "created_at",
    "calendar_interval": "day",
    "min_doc_count": 0
  }
}
```

### 5.6.5 学历与经验分布

```json
"education_dist": {
  "terms": { "field": "education", "size": 10 }
}

"experience_dist": {
  "terms": { "field": "experience", "size": 10 }
}
```

### 5.6.6 热门技能

延续当前逻辑，合并：

- `skills`
- `ai_skills`

再走一次噪声过滤。

---

## 5.7 统一返回结构

建议城市页和行业页返回同一结构：

```json
{
  "dimension": "city",
  "left": {
    "id": 101020100,
    "name": "上海",
    "overview": {
      "sample_size": 18234,
      "salary_avg": 21800,
      "salary_median": 20000,
      "salary_p25": 15000,
      "salary_p75": 28000,
      "high_salary_ratio": 0.26,
      "job_count_30d": 18234
    },
    "salary_distribution": [],
    "trend": [],
    "education_distribution": [],
    "experience_distribution": [],
    "top_skills": []
  },
  "right": {
    "id": 101280100,
    "name": "深圳",
    "overview": {},
    "salary_distribution": [],
    "trend": [],
    "education_distribution": [],
    "experience_distribution": [],
    "top_skills": []
  },
  "summary": {
    "winner_dimension": "right",
    "salary_gap": 1800,
    "sample_gap": 3520,
    "insight": "深圳薪资更高，但样本集中在 3-5 年经验岗位。"
  }
}
```

这样前端只需要做一个通用渲染组件。

---

## 6. API 设计

## 6.1 城市对比接口

`GET /analysis/compare/cities`

参数：

- `keyword`
- `left_city_code`
- `right_city_code`
- `industry`
- `industry_2`
- `experience`
- `education`
- `days`

## 6.2 行业对比接口

`GET /analysis/compare/industries`

参数：

- `keyword`
- `left_industry_code`
- `right_industry_code`
- `city_code`
- `experience`
- `education`
- `days`

## 6.3 可选统一接口

如果想进一步抽象，也可以提供：

`GET /analysis/compare`

参数：

- `dimension=city|industry`
- `left_value`
- `right_value`
- 其他公共参数

第一期不建议只暴露统一接口。

原因：

- 前端可读性差
- 文档不直观
- 面试展示时不如两个清晰接口好讲

更合理方式：

- 控制器层暴露两个清晰接口
- 服务层内部统一走 `compare_dimension()`

---

## 7. Schema 设计建议

建议在 `jobCollectionWebApi/schemas/analysis_schema.py` 新增：

```python
class CompareOverview(BaseModel):
    sample_size: int
    salary_avg: float = 0
    salary_median: float = 0
    salary_p25: float = 0
    salary_p75: float = 0
    high_salary_ratio: float = 0
    job_count_30d: int = 0


class CompareBucketItem(BaseModel):
    name: str
    value: float | int


class CompareTrendItem(BaseModel):
    date: str
    value: int


class CompareSideResult(BaseModel):
    id: int
    name: str
    overview: CompareOverview
    salary_distribution: List[CompareBucketItem]
    trend: List[CompareTrendItem]
    education_distribution: List[CompareBucketItem]
    experience_distribution: List[CompareBucketItem]
    top_skills: List[CompareBucketItem]


class CompareSummary(BaseModel):
    winner_dimension: Optional[str] = None
    salary_gap: float = 0
    sample_gap: int = 0
    insight: str = ""


class CompareAnalysisResponse(BaseModel):
    dimension: str
    left: CompareSideResult
    right: CompareSideResult
    summary: CompareSummary
```

---

## 8. 前端设计

## 8.1 新页面

建议新增：

- `frontend/src/views/CitySalaryCompareView.vue`
- `frontend/src/views/IndustrySalaryCompareView.vue`

## 8.2 通用组件

建议抽公共组件：

- `frontend/src/components/compare/CompareFilterBar.vue`
- `frontend/src/components/compare/CompareOverviewCards.vue`
- `frontend/src/components/compare/CompareSalaryChart.vue`
- `frontend/src/components/compare/CompareTrendChart.vue`
- `frontend/src/components/compare/CompareSkillChart.vue`

如果你想更快上线，也可以第一期只建一个：

- `frontend/src/components/compare/CompareDashboard.vue`

由页面传入：

- `dimension`
- `query params`
- `left/right labels`

---

## 8.3 前端 API

`frontend/src/api/analysis.js` 增加：

```javascript
getCitySalaryCompare(params) {
  return api.get("/analysis/compare/cities", { params });
},

getIndustrySalaryCompare(params) {
  return api.get("/analysis/compare/industries", { params });
},
```

---

## 8.4 路由

建议新增：

```javascript
{
  path: "compare/cities",
  name: "compare-cities",
  component: () => import("@/views/CitySalaryCompareView.vue"),
},
{
  path: "compare/industries",
  name: "compare-industries",
  component: () => import("@/views/IndustrySalaryCompareView.vue"),
},
```

---

## 9. 缓存策略

建议缓存 10 分钟：

- 城市对比：`analysis:compare:cities:v1`
- 行业对比：`analysis:compare:industries:v1`

缓存 key 需要纳入：

- 左右对比对象
- 关键词
- 经验
- 学历
- 行业/城市额外过滤
- `days`

注意：

- 左右对比顺序不能被忽略
- `上海 vs 深圳` 和 `深圳 vs 上海` 不应该共用一份结果

---

## 10. 降级与异常策略

第一期建议：

- ES 查询失败时直接返回空结构，不走 PostgreSQL 降级

原因：

- 这是聚合分析接口，不是核心交易接口
- PostgreSQL 即时聚合实现成本高，且统计口径容易不一致
- 现在目标是快速成型，不是双链路完备

前端展示策略：

- 无数据时显示“样本不足或暂未生成”
- 不要让页面报错白屏

---

## 11. 面试展示亮点

这个功能做完后，你可以明确讲以下点：

1. 统一对比分析服务层，避免城市页和行业页逻辑重复
2. 用 ES 聚合完成中位数、分位数、趋势、分布统计
3. 结果统一建模，前端复用通用对比组件
4. 以“求职决策”作为产品目标，而不是单纯图表展示
5. 通过缓存控制聚合开销

---

## 12. 推荐开发顺序

### Phase 1

先做统一服务层和城市对比接口：

1. `comparison_analysis_service.py`
2. `/analysis/compare/cities`
3. `CitySalaryCompareView.vue`

原因：

- 城市维度口径最稳定
- 页面更直观
- 适合尽快出可演示版本

### Phase 2

复用同一套服务做行业对比：

1. `/analysis/compare/industries`
2. `IndustrySalaryCompareView.vue`

### Phase 3

补 AI 总结卡和更丰富的结论文案。

---

## 13. 第一版最小可交付范围

如果时间只有 1 到 2 天，建议只做这些：

### 城市页

- 两个城市选择
- 岗位关键词输入
- 中位数/平均值/样本量
- 薪资分布图
- 趋势图

### 行业页

- 两个一级行业选择
- 岗位关键词输入
- 中位数/平均值/样本量
- 薪资分布图

### 服务层

- 公共过滤条件
- 单次 ES 双侧聚合
- 统一响应结构
- 10 分钟缓存

---

## 14. 不建议第一期做的内容

- 城市和行业同时支持多选
- 支持任意数量对象群组对比
- PostgreSQL 降级统计
- 复杂 AI 报告生成
- 导出 PDF
- 多页签复杂交互

这些都会拖慢上线，不适合你当前目标。

---

## 15. 最终建议

这 3 个方案里，真正的核心不是两个页面，而是统一服务层。

正确关系应该是：

- 城市薪资对比页 = 统一服务层 + 城市维度参数
- 行业薪资对比页 = 统一服务层 + 行业维度参数

如果服务层先设计好，后面你扩展：

- 学历对比
- 经验对比
- 岗位方向对比

都会非常顺。

